## **Fitbit/Google Pixel Watch Data Parser**

**Instructions for Running the Script:**
   - Ensure you have **Python 3** installed.
   - Install the required Python libraries by running:
     ```bash
     pip install pandas tqdm
     ```
   - Place the `parser.py` script in your working directory.
   - Ensure your Fitbit/Google Pixel Watch data files are placed in the appropriate directory (usually `Physical Activity`).
   - Run the script:
     ```bash
     python3 parser.py
     ```
   - The **output CSV files** will be generated in the `output/` directory.

The current version of the parser allows you to run it by typing:

```bash
python3 parser.py
```

**Current Functionality:**
- The parser extracts data from various Fitbit/Google Pixel Watch JSON files (such as `heart_rate-*.json`, `steps-*.json`, etc.).
- It converts the data into **CSV files** with the following categories:
  - Heart Rate
  - Calories
  - Steps
  - Distance
- The extracted data is saved in the `output/` directory as CSV files with the labels `heartrate.csv`, `calories.csv`, `steps.csv`, and `distance.csv`.

**Known Issue (Current Limitation):**
- The **Date and Time** information for each record is missing from the CSV output.

---

### **Next Steps**


1. **Making a Pipeline to Direct to the Original R File:**
   - You can **integrate the Python parser output** directly into your original **R script (`DataProcessor.R`)**:
     - Instead of reading JSON files, modify the R script to **read the CSV files** generated by the Python parser.
     - The CSVs (`heartrate.csv`, `calories.csv`, etc.) will contain the same structure expected by the R script.
     - Modify the R script to skip the JSON parsing section and load the CSVs:
       ```r
       heartrate <- read.csv("output/heartrate.csv")
       calories <- read.csv("output/calories.csv")
       steps <- read.csv("output/steps.csv")
       distance <- read.csv("output/distance.csv")
       ```
     - The R script will then proceed to merge and process the data just like before.

2. **What Will Be Fixed Next:**
   - The **CSV merging** functionality to combine all the CSV files into a single `fitbit_data.csv` that contains all extracted data.
   - Handle **file path errors** and **data validation** to ensure robustness in case of incomplete or corrupt data files.
   - Add a **progress bar** with `tqdm` to visually track the progress during parsing.

3. **Future Improvements:**
   - **More Data Categories**: Add support for other Fitbit/Google Pixel Watch data files such as **sleep data**, **exercise sessions**, or **active minutes**.
   - Improve **performance** for large datasets by leveraging parallel processing for file parsing.
